/**
 * PROMPT 17: "오늘의 학습" 예상 소요 시간 계산 상수(이 문서에서 확정).
 * 계획서는 "약 32분" 형태의 예시만 제시하고 계산식을 명시하지 않아,
 * 단어당 평균 약 35초(뜻 확인 + 평가)를 가정값으로 채택한다.
 */
export const SECONDS_PER_WORD = 35;

/**
 * "모르겠음/헷갈림" 카드를 세션 큐 끝에 다시 넣어 즉시 재출제하는 횟수 상한(카드당). 무제한
 * 재출제 시 계속 못 외우는 카드가 세션을 끝없이 늘릴 수 있어, 반복해도 안 외워지면 세션 완료
 * 후 "틀린 것만 다시 풀기"로 넘긴다.
 */
export const MAX_SESSION_REQUEUE_PER_CARD = 1;

/**
 * "오늘의 한자"(PROMPT 36)에 포함할 신규 한자 상한. 단어의 `daily_word_target`처럼 사용자가
 * 설정할 수 있는 값이 아니라, 이 문서가 고정값으로 확정한다(한자는 온보딩에서 목표를 받지
 * 않으므로 온보딩 확장 없이 즉시 값이 필요했다).
 */
export const DAILY_KANJI_TARGET = 10;
